import React from 'react';
import PropTypes from 'prop-types';

const General = () => {
    return (
        <div>
            General Page
        </div>
    );
};

General.propTypes = {};

export default General;